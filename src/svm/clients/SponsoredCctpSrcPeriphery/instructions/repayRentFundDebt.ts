/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const REPAY_RENT_FUND_DEBT_DISCRIMINATOR = new Uint8Array([
  111, 95, 222, 174, 241, 41, 61, 78,
]);

export function getRepayRentFundDebtDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REPAY_RENT_FUND_DEBT_DISCRIMINATOR
  );
}

export type RepayRentFundDebtInstruction<
  TProgram extends string = typeof SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS,
  TAccountRentFund extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountRentClaim extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRentFund extends string
        ? WritableAccount<TAccountRentFund>
        : TAccountRentFund,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountRentClaim extends string
        ? WritableAccount<TAccountRentClaim>
        : TAccountRentClaim,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RepayRentFundDebtInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RepayRentFundDebtInstructionDataArgs = {};

export function getRepayRentFundDebtInstructionDataEncoder(): Encoder<RepayRentFundDebtInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REPAY_RENT_FUND_DEBT_DISCRIMINATOR })
  );
}

export function getRepayRentFundDebtInstructionDataDecoder(): Decoder<RepayRentFundDebtInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRepayRentFundDebtInstructionDataCodec(): Codec<
  RepayRentFundDebtInstructionDataArgs,
  RepayRentFundDebtInstructionData
> {
  return combineCodec(
    getRepayRentFundDebtInstructionDataEncoder(),
    getRepayRentFundDebtInstructionDataDecoder()
  );
}

export type RepayRentFundDebtAsyncInput<
  TAccountRentFund extends string = string,
  TAccountRecipient extends string = string,
  TAccountRentClaim extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  rentFund?: Address<TAccountRentFund>;
  recipient: Address<TAccountRecipient>;
  rentClaim?: Address<TAccountRentClaim>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export async function getRepayRentFundDebtInstructionAsync<
  TAccountRentFund extends string,
  TAccountRecipient extends string,
  TAccountRentClaim extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address =
    typeof SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS,
>(
  input: RepayRentFundDebtAsyncInput<
    TAccountRentFund,
    TAccountRecipient,
    TAccountRentClaim,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RepayRentFundDebtInstruction<
    TProgramAddress,
    TAccountRentFund,
    TAccountRecipient,
    TAccountRentClaim,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    rentFund: { value: input.rentFund ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
    rentClaim: { value: input.rentClaim ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.rentFund.value) {
    accounts.rentFund.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 110, 116, 95, 102, 117, 110, 100])
        ),
      ],
    });
  }
  if (!accounts.rentClaim.value) {
    accounts.rentClaim.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 110, 116, 95, 99, 108, 97, 105, 109])
        ),
        getAddressEncoder().encode(expectAddress(accounts.recipient.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.rentFund),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.rentClaim),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    programAddress,
    data: getRepayRentFundDebtInstructionDataEncoder().encode({}),
  } as RepayRentFundDebtInstruction<
    TProgramAddress,
    TAccountRentFund,
    TAccountRecipient,
    TAccountRentClaim,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >;

  return instruction;
}

export type RepayRentFundDebtInput<
  TAccountRentFund extends string = string,
  TAccountRecipient extends string = string,
  TAccountRentClaim extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  rentFund: Address<TAccountRentFund>;
  recipient: Address<TAccountRecipient>;
  rentClaim: Address<TAccountRentClaim>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export function getRepayRentFundDebtInstruction<
  TAccountRentFund extends string,
  TAccountRecipient extends string,
  TAccountRentClaim extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address =
    typeof SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS,
>(
  input: RepayRentFundDebtInput<
    TAccountRentFund,
    TAccountRecipient,
    TAccountRentClaim,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RepayRentFundDebtInstruction<
  TProgramAddress,
  TAccountRentFund,
  TAccountRecipient,
  TAccountRentClaim,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    rentFund: { value: input.rentFund ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
    rentClaim: { value: input.rentClaim ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.rentFund),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.rentClaim),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    programAddress,
    data: getRepayRentFundDebtInstructionDataEncoder().encode({}),
  } as RepayRentFundDebtInstruction<
    TProgramAddress,
    TAccountRentFund,
    TAccountRecipient,
    TAccountRentClaim,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >;

  return instruction;
}

export type ParsedRepayRentFundDebtInstruction<
  TProgram extends string = typeof SPONSORED_CCTP_SRC_PERIPHERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    rentFund: TAccountMetas[0];
    recipient: TAccountMetas[1];
    rentClaim: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: RepayRentFundDebtInstructionData;
};

export function parseRepayRentFundDebtInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRepayRentFundDebtInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      rentFund: getNextAccount(),
      recipient: getNextAccount(),
      rentClaim: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getRepayRentFundDebtInstructionDataDecoder().decode(instruction.data),
  };
}
