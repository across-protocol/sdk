name: CI
on: [push]
jobs:
  build:
    name: Builds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
      - run: yarn install
      - run: yarn build
